============================= test session starts =============================
platform win32 -- Python 3.13.2, pytest-7.4.3, pluggy-1.6.0 -- c:\PMS\PMS2025\Inscr-Meta\prg-Meta\Eventos-MetaReciclagem\.venv\Scripts\python.exe
cachedir: .pytest_cache
django: version: 5.2.4, settings: config.settings (from option)
rootdir: C:\PMS\PMS2025\Inscr-Meta\prg-Meta\Eventos-MetaReciclagem\eventosmeta
configfile: pytest.ini
plugins: Faker-40.11.1, cov-4.1.0, django-4.7.0, mock-3.15.1
collecting ... collected 17 items

apps/academico/tests/test_models.py::TestStatusMatricula::test_status_criado_corretamente PASSED [  5%]
apps/academico/tests/test_models.py::TestStatusMatricula::test_status_get_or_create_evita_duplicata PASSED [ 11%]
apps/academico/tests/test_models.py::TestMatricula::test_matricula_criada_corretamente PASSED [ 17%]
apps/academico/tests/test_models.py::TestMatricula::test_matricula_data_preenchida PASSED [ 23%]
apps/academico/tests/test_models.py::TestMatricula::test_matricula_status_ativo_default PASSED [ 29%]
apps/academico/tests/test_models.py::TestMatricula::test_matricula_unique_together_turma_interessado PASSED [ 35%]
apps/academico/tests/test_models.py::TestAvaliacao::test_avaliacao_aprovado_quando_nota_maior_7 FAILED [ 41%]
apps/academico/tests/test_models.py::TestAvaliacao::test_avaliacao_criada_corretamente FAILED [ 47%]
apps/academico/tests/test_models.py::TestAvaliacao::test_avaliacao_reprovado_quando_nota_menor_7 FAILED [ 52%]
apps/academico/tests/test_services.py::TestMatriculaService::test_alterar_status_matricula FAILED [ 58%]
apps/academico/tests/test_services.py::TestMatriculaService::test_avaliar_aluno_aprova_com_nota_alta FAILED [ 64%]
apps/academico/tests/test_services.py::TestMatriculaService::test_avaliar_aluno_reprova_com_nota_baixa FAILED [ 70%]
apps/academico/tests/test_services.py::TestMatriculaService::test_gerar_relatorio_turma FAILED [ 76%]
apps/academico/tests/test_services.py::TestMatriculaService::test_matricula_gerada_com_numero_sequencial FAILED [ 82%]
apps/academico/tests/test_services.py::TestMatriculaService::test_matricular_classificado_cria_matricula FAILED [ 88%]
apps/academico/tests/test_services.py::TestMatriculaService::test_verificar_disponibilidade_turma_com_vagas FAILED [ 94%]
apps/academico/tests/test_services.py::TestMatriculaService::test_verificar_disponibilidade_turma_sem_vagas FAILED [100%]

================================== FAILURES ===================================
__________ TestAvaliacao.test_avaliacao_aprovado_quando_nota_maior_7 __________

self = <django.db.backends.utils.CursorWrapper object at 0x0000025D5A5F6F30>
sql = 'INSERT INTO "academico_avaliacao" ("matricula_id", "nota_final", "frequencia", "aprovado", "observacoes", "certificad...ado", "avaliado_em", "atualizado_em") VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s) RETURNING "academico_avaliacao"."id"'
params = (5, Decimal('8'), Decimal('62'), True, 'Friend summer phone on process a discussion. Red down report better. Page foll...\nTrial base sea end. Gun people rich type white remember. Else enough off miss issue rise night western.', False, ...)
ignored_wrapper_args = (False, {'connection': <DatabaseWrapper vendor='postgresql' alias='default'>, 'cursor': <django.db.backends.utils.CursorWrapper object at 0x0000025D5A5F6F30>})

    def _execute(self, sql, params, *ignored_wrapper_args):
        # Raise a warning during app initialization (stored_app_configs is only
        # ever set during testing).
        if not apps.ready and not apps.stored_app_configs:
            warnings.warn(self.APPS_NOT_READY_WARNING_MSG, category=RuntimeWarning)
        self.db.validate_no_broken_transaction()
        with self.db.wrap_database_errors:
            if params is None:
                # params default might be backend specific.
                return self.cursor.execute(sql)
            else:
>               return self.cursor.execute(sql, params)
E               psycopg2.errors.UniqueViolation: ERRO:  duplicar valor da chave viola a restriþÒo de unicidade "academico_avaliacao_matricula_id_key"
E               DETAIL:  Chave (matricula_id)=(5) jß existe.

..\.venv\Lib\site-packages\django\db\backends\utils.py:105: UniqueViolation

The above exception was the direct cause of the following exception:

self = <apps.academico.tests.test_models.TestAvaliacao testMethod=test_avaliacao_aprovado_quando_nota_maior_7>

    def test_avaliacao_aprovado_quando_nota_maior_7(self):
>       avaliacao = AvaliacaoFactory(nota_final=8)

apps\academico\tests\test_models.py:55: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:663: in create
    obj.save(force_insert=True, using=self.db)
..\.venv\Lib\site-packages\django\db\models\base.py:902: in save
    self.save_base(
..\.venv\Lib\site-packages\django\db\models\base.py:1008: in save_base
    updated = self._save_table(
..\.venv\Lib\site-packages\django\db\models\base.py:1169: in _save_table
    results = self._do_insert(
..\.venv\Lib\site-packages\django\db\models\base.py:1210: in _do_insert
    return manager._insert(
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:1868: in _insert
    return query.get_compiler(using=using).execute_sql(returning_fields)
..\.venv\Lib\site-packages\django\db\models\sql\compiler.py:1882: in execute_sql
    cursor.execute(sql, params)
..\.venv\Lib\site-packages\django\db\backends\utils.py:79: in execute
    return self._execute_with_wrappers(
..\.venv\Lib\site-packages\django\db\backends\utils.py:92: in _execute_with_wrappers
    return executor(sql, params, many, context)
..\.venv\Lib\site-packages\django\db\backends\utils.py:100: in _execute
    with self.db.wrap_database_errors:
..\.venv\Lib\site-packages\django\db\utils.py:91: in __exit__
    raise dj_exc_value.with_traceback(traceback) from exc_value
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <django.db.backends.utils.CursorWrapper object at 0x0000025D5A5F6F30>
sql = 'INSERT INTO "academico_avaliacao" ("matricula_id", "nota_final", "frequencia", "aprovado", "observacoes", "certificad...ado", "avaliado_em", "atualizado_em") VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s) RETURNING "academico_avaliacao"."id"'
params = (5, Decimal('8'), Decimal('62'), True, 'Friend summer phone on process a discussion. Red down report better. Page foll...\nTrial base sea end. Gun people rich type white remember. Else enough off miss issue rise night western.', False, ...)
ignored_wrapper_args = (False, {'connection': <DatabaseWrapper vendor='postgresql' alias='default'>, 'cursor': <django.db.backends.utils.CursorWrapper object at 0x0000025D5A5F6F30>})

    def _execute(self, sql, params, *ignored_wrapper_args):
        # Raise a warning during app initialization (stored_app_configs is only
        # ever set during testing).
        if not apps.ready and not apps.stored_app_configs:
            warnings.warn(self.APPS_NOT_READY_WARNING_MSG, category=RuntimeWarning)
        self.db.validate_no_broken_transaction()
        with self.db.wrap_database_errors:
            if params is None:
                # params default might be backend specific.
                return self.cursor.execute(sql)
            else:
>               return self.cursor.execute(sql, params)
E               django.db.utils.IntegrityError: ERRO:  duplicar valor da chave viola a restriþÒo de unicidade "academico_avaliacao_matricula_id_key"
E               DETAIL:  Chave (matricula_id)=(5) jß existe.

..\.venv\Lib\site-packages\django\db\backends\utils.py:105: IntegrityError
______________ TestAvaliacao.test_avaliacao_criada_corretamente _______________

self = <django.db.backends.utils.CursorWrapper object at 0x0000025D5AECA4B0>
sql = 'INSERT INTO "academico_avaliacao" ("matricula_id", "nota_final", "frequencia", "aprovado", "observacoes", "certificad...ado", "avaliado_em", "atualizado_em") VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s) RETURNING "academico_avaliacao"."id"'
params = (6, Decimal('5'), Decimal('60'), False, 'Expert such see officer. Big it hair why. Once trade score agreement Mr. Seek particular program course cold system pick.', False, ...)
ignored_wrapper_args = (False, {'connection': <DatabaseWrapper vendor='postgresql' alias='default'>, 'cursor': <django.db.backends.utils.CursorWrapper object at 0x0000025D5AECA4B0>})

    def _execute(self, sql, params, *ignored_wrapper_args):
        # Raise a warning during app initialization (stored_app_configs is only
        # ever set during testing).
        if not apps.ready and not apps.stored_app_configs:
            warnings.warn(self.APPS_NOT_READY_WARNING_MSG, category=RuntimeWarning)
        self.db.validate_no_broken_transaction()
        with self.db.wrap_database_errors:
            if params is None:
                # params default might be backend specific.
                return self.cursor.execute(sql)
            else:
>               return self.cursor.execute(sql, params)
E               psycopg2.errors.UniqueViolation: ERRO:  duplicar valor da chave viola a restriþÒo de unicidade "academico_avaliacao_matricula_id_key"
E               DETAIL:  Chave (matricula_id)=(6) jß existe.

..\.venv\Lib\site-packages\django\db\backends\utils.py:105: UniqueViolation

The above exception was the direct cause of the following exception:

self = <apps.academico.tests.test_models.TestAvaliacao testMethod=test_avaliacao_criada_corretamente>

    def test_avaliacao_criada_corretamente(self):
>       avaliacao = AvaliacaoFactory()

apps\academico\tests\test_models.py:47: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:663: in create
    obj.save(force_insert=True, using=self.db)
..\.venv\Lib\site-packages\django\db\models\base.py:902: in save
    self.save_base(
..\.venv\Lib\site-packages\django\db\models\base.py:1008: in save_base
    updated = self._save_table(
..\.venv\Lib\site-packages\django\db\models\base.py:1169: in _save_table
    results = self._do_insert(
..\.venv\Lib\site-packages\django\db\models\base.py:1210: in _do_insert
    return manager._insert(
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:1868: in _insert
    return query.get_compiler(using=using).execute_sql(returning_fields)
..\.venv\Lib\site-packages\django\db\models\sql\compiler.py:1882: in execute_sql
    cursor.execute(sql, params)
..\.venv\Lib\site-packages\django\db\backends\utils.py:79: in execute
    return self._execute_with_wrappers(
..\.venv\Lib\site-packages\django\db\backends\utils.py:92: in _execute_with_wrappers
    return executor(sql, params, many, context)
..\.venv\Lib\site-packages\django\db\backends\utils.py:100: in _execute
    with self.db.wrap_database_errors:
..\.venv\Lib\site-packages\django\db\utils.py:91: in __exit__
    raise dj_exc_value.with_traceback(traceback) from exc_value
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <django.db.backends.utils.CursorWrapper object at 0x0000025D5AECA4B0>
sql = 'INSERT INTO "academico_avaliacao" ("matricula_id", "nota_final", "frequencia", "aprovado", "observacoes", "certificad...ado", "avaliado_em", "atualizado_em") VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s) RETURNING "academico_avaliacao"."id"'
params = (6, Decimal('5'), Decimal('60'), False, 'Expert such see officer. Big it hair why. Once trade score agreement Mr. Seek particular program course cold system pick.', False, ...)
ignored_wrapper_args = (False, {'connection': <DatabaseWrapper vendor='postgresql' alias='default'>, 'cursor': <django.db.backends.utils.CursorWrapper object at 0x0000025D5AECA4B0>})

    def _execute(self, sql, params, *ignored_wrapper_args):
        # Raise a warning during app initialization (stored_app_configs is only
        # ever set during testing).
        if not apps.ready and not apps.stored_app_configs:
            warnings.warn(self.APPS_NOT_READY_WARNING_MSG, category=RuntimeWarning)
        self.db.validate_no_broken_transaction()
        with self.db.wrap_database_errors:
            if params is None:
                # params default might be backend specific.
                return self.cursor.execute(sql)
            else:
>               return self.cursor.execute(sql, params)
E               django.db.utils.IntegrityError: ERRO:  duplicar valor da chave viola a restriþÒo de unicidade "academico_avaliacao_matricula_id_key"
E               DETAIL:  Chave (matricula_id)=(6) jß existe.

..\.venv\Lib\site-packages\django\db\backends\utils.py:105: IntegrityError
_________ TestAvaliacao.test_avaliacao_reprovado_quando_nota_menor_7 __________

self = <django.db.backends.utils.CursorWrapper object at 0x0000025D5B5BA0F0>
sql = 'INSERT INTO "academico_avaliacao" ("matricula_id", "nota_final", "frequencia", "aprovado", "observacoes", "certificad...ado", "avaliado_em", "atualizado_em") VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s) RETURNING "academico_avaliacao"."id"'
params = (7, Decimal('5'), Decimal('40'), False, 'Cultural cost imagine could. Skin defense whole key choose north son.\nEither point number early young per finish. Partner war continue dog.', False, ...)
ignored_wrapper_args = (False, {'connection': <DatabaseWrapper vendor='postgresql' alias='default'>, 'cursor': <django.db.backends.utils.CursorWrapper object at 0x0000025D5B5BA0F0>})

    def _execute(self, sql, params, *ignored_wrapper_args):
        # Raise a warning during app initialization (stored_app_configs is only
        # ever set during testing).
        if not apps.ready and not apps.stored_app_configs:
            warnings.warn(self.APPS_NOT_READY_WARNING_MSG, category=RuntimeWarning)
        self.db.validate_no_broken_transaction()
        with self.db.wrap_database_errors:
            if params is None:
                # params default might be backend specific.
                return self.cursor.execute(sql)
            else:
>               return self.cursor.execute(sql, params)
E               psycopg2.errors.UniqueViolation: ERRO:  duplicar valor da chave viola a restriþÒo de unicidade "academico_avaliacao_matricula_id_key"
E               DETAIL:  Chave (matricula_id)=(7) jß existe.

..\.venv\Lib\site-packages\django\db\backends\utils.py:105: UniqueViolation

The above exception was the direct cause of the following exception:

self = <apps.academico.tests.test_models.TestAvaliacao testMethod=test_avaliacao_reprovado_quando_nota_menor_7>

    def test_avaliacao_reprovado_quando_nota_menor_7(self):
>       avaliacao = AvaliacaoFactory(nota_final=5)

apps\academico\tests\test_models.py:59: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:663: in create
    obj.save(force_insert=True, using=self.db)
..\.venv\Lib\site-packages\django\db\models\base.py:902: in save
    self.save_base(
..\.venv\Lib\site-packages\django\db\models\base.py:1008: in save_base
    updated = self._save_table(
..\.venv\Lib\site-packages\django\db\models\base.py:1169: in _save_table
    results = self._do_insert(
..\.venv\Lib\site-packages\django\db\models\base.py:1210: in _do_insert
    return manager._insert(
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:1868: in _insert
    return query.get_compiler(using=using).execute_sql(returning_fields)
..\.venv\Lib\site-packages\django\db\models\sql\compiler.py:1882: in execute_sql
    cursor.execute(sql, params)
..\.venv\Lib\site-packages\django\db\backends\utils.py:79: in execute
    return self._execute_with_wrappers(
..\.venv\Lib\site-packages\django\db\backends\utils.py:92: in _execute_with_wrappers
    return executor(sql, params, many, context)
..\.venv\Lib\site-packages\django\db\backends\utils.py:100: in _execute
    with self.db.wrap_database_errors:
..\.venv\Lib\site-packages\django\db\utils.py:91: in __exit__
    raise dj_exc_value.with_traceback(traceback) from exc_value
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <django.db.backends.utils.CursorWrapper object at 0x0000025D5B5BA0F0>
sql = 'INSERT INTO "academico_avaliacao" ("matricula_id", "nota_final", "frequencia", "aprovado", "observacoes", "certificad...ado", "avaliado_em", "atualizado_em") VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s) RETURNING "academico_avaliacao"."id"'
params = (7, Decimal('5'), Decimal('40'), False, 'Cultural cost imagine could. Skin defense whole key choose north son.\nEither point number early young per finish. Partner war continue dog.', False, ...)
ignored_wrapper_args = (False, {'connection': <DatabaseWrapper vendor='postgresql' alias='default'>, 'cursor': <django.db.backends.utils.CursorWrapper object at 0x0000025D5B5BA0F0>})

    def _execute(self, sql, params, *ignored_wrapper_args):
        # Raise a warning during app initialization (stored_app_configs is only
        # ever set during testing).
        if not apps.ready and not apps.stored_app_configs:
            warnings.warn(self.APPS_NOT_READY_WARNING_MSG, category=RuntimeWarning)
        self.db.validate_no_broken_transaction()
        with self.db.wrap_database_errors:
            if params is None:
                # params default might be backend specific.
                return self.cursor.execute(sql)
            else:
>               return self.cursor.execute(sql, params)
E               django.db.utils.IntegrityError: ERRO:  duplicar valor da chave viola a restriþÒo de unicidade "academico_avaliacao_matricula_id_key"
E               DETAIL:  Chave (matricula_id)=(7) jß existe.

..\.venv\Lib\site-packages\django\db\backends\utils.py:105: IntegrityError
_____________ TestMatriculaService.test_alterar_status_matricula ______________

self = <apps.academico.tests.test_services.TestMatriculaService testMethod=test_alterar_status_matricula>

    def setUp(self):
>       self.turma_com_vagas = TurmaFactory(total_vagas=5)

apps\academico\tests\test_services.py:10: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:661: in create
    obj = self.model(**kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <Turma: board - Turma 7>, args = (), kwargs = {'total_vagas': 5}
cls = <class 'apps.eventos.models.Turma'>, opts = <Options for Turma>
_setattr = <built-in function setattr>, _DEFERRED = <Deferred field>
fields_iter = <tuple_iterator object at 0x0000025D5A9FBAC0>, val = None
field = <django.db.models.fields.DateTimeField: atualizado_em>
is_related_object = False, rel_obj = <Evento: board>
property_names = frozenset({'pk'})

    def __init__(self, *args, **kwargs):
        # Alias some things as locals to avoid repeat global lookups
        cls = self.__class__
        opts = self._meta
        _setattr = setattr
        _DEFERRED = DEFERRED
        if opts.abstract:
            raise TypeError("Abstract models cannot be instantiated.")
    
        pre_init.send(sender=cls, args=args, kwargs=kwargs)
    
        # Set up the storage for instance state
        self._state = ModelState()
    
        # There is a rather weird disparity here; if kwargs, it's set, then args
        # overrides it. It should be one or the other; don't duplicate the work
        # The reason for the kwargs check is that standard iterator passes in by
        # args, and instantiation for iteration is 33% faster.
        if len(args) > len(opts.concrete_fields):
            # Daft, but matches old exception sans the err msg.
            raise IndexError("Number of args exceeds number of fields")
    
        if not kwargs:
            fields_iter = iter(opts.concrete_fields)
            # The ordering of the zip calls matter - zip throws StopIteration
            # when an iter throws it. So if the first iter throws it, the second
            # is *not* consumed. We rely on this, so don't change the order
            # without changing the logic.
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
        else:
            # Slower, kwargs-ready version.
            fields_iter = iter(opts.fields)
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
                if kwargs.pop(field.name, NOT_PROVIDED) is not NOT_PROVIDED:
                    raise TypeError(
                        f"{cls.__qualname__}() got both positional and "
                        f"keyword arguments for field '{field.name}'."
                    )
    
        # Now we're left with the unprocessed fields that *must* come from
        # keywords, or default.
    
        for field in fields_iter:
            is_related_object = False
            # Virtual field
            if field.column is None or field.generated:
                continue
            if kwargs:
                if isinstance(field.remote_field, ForeignObjectRel):
                    try:
                        # Assume object instance was passed in.
                        rel_obj = kwargs.pop(field.name)
                        is_related_object = True
                    except KeyError:
                        try:
                            # Object instance wasn't passed in -- must be an ID.
                            val = kwargs.pop(field.attname)
                        except KeyError:
                            val = field.get_default()
                else:
                    try:
                        val = kwargs.pop(field.attname)
                    except KeyError:
                        # This is done with an exception rather than the
                        # default argument on pop because we don't want
                        # get_default() to be evaluated, and then not used.
                        # Refs #12057.
                        val = field.get_default()
            else:
                val = field.get_default()
    
            if is_related_object:
                # If we are passed a related instance, set it using the
                # field.name instead of field.attname (e.g. "user" instead of
                # "user_id") so that the object gets properly cached (and type
                # checked) by the RelatedObjectDescriptor.
                if rel_obj is not _DEFERRED:
                    _setattr(self, field.name, rel_obj)
            else:
                if val is not _DEFERRED:
                    _setattr(self, field.attname, val)
    
        if kwargs:
            property_names = opts._property_names
            unexpected = ()
            for prop, value in kwargs.items():
                # Any remaining kwargs must correspond to properties or virtual
                # fields.
                if prop in property_names:
                    if value is not _DEFERRED:
                        _setattr(self, prop, value)
                else:
                    try:
                        opts.get_field(prop)
                    except FieldDoesNotExist:
                        unexpected += (prop,)
                    else:
                        if value is not _DEFERRED:
                            _setattr(self, prop, value)
            if unexpected:
                unexpected_names = ", ".join(repr(n) for n in unexpected)
>               raise TypeError(
                    f"{cls.__name__}() got unexpected keyword arguments: "
                    f"{unexpected_names}"
                )
E               TypeError: Turma() got unexpected keyword arguments: 'total_vagas'

..\.venv\Lib\site-packages\django\db\models\base.py:569: TypeError
________ TestMatriculaService.test_avaliar_aluno_aprova_com_nota_alta _________

self = <apps.academico.tests.test_services.TestMatriculaService testMethod=test_avaliar_aluno_aprova_com_nota_alta>

    def setUp(self):
>       self.turma_com_vagas = TurmaFactory(total_vagas=5)

apps\academico\tests\test_services.py:10: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:661: in create
    obj = self.model(**kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <Turma: level - Turma 8>, args = (), kwargs = {'total_vagas': 5}
cls = <class 'apps.eventos.models.Turma'>, opts = <Options for Turma>
_setattr = <built-in function setattr>, _DEFERRED = <Deferred field>
fields_iter = <tuple_iterator object at 0x0000025D5A9FBE80>, val = None
field = <django.db.models.fields.DateTimeField: atualizado_em>
is_related_object = False, rel_obj = <Evento: level>
property_names = frozenset({'pk'})

    def __init__(self, *args, **kwargs):
        # Alias some things as locals to avoid repeat global lookups
        cls = self.__class__
        opts = self._meta
        _setattr = setattr
        _DEFERRED = DEFERRED
        if opts.abstract:
            raise TypeError("Abstract models cannot be instantiated.")
    
        pre_init.send(sender=cls, args=args, kwargs=kwargs)
    
        # Set up the storage for instance state
        self._state = ModelState()
    
        # There is a rather weird disparity here; if kwargs, it's set, then args
        # overrides it. It should be one or the other; don't duplicate the work
        # The reason for the kwargs check is that standard iterator passes in by
        # args, and instantiation for iteration is 33% faster.
        if len(args) > len(opts.concrete_fields):
            # Daft, but matches old exception sans the err msg.
            raise IndexError("Number of args exceeds number of fields")
    
        if not kwargs:
            fields_iter = iter(opts.concrete_fields)
            # The ordering of the zip calls matter - zip throws StopIteration
            # when an iter throws it. So if the first iter throws it, the second
            # is *not* consumed. We rely on this, so don't change the order
            # without changing the logic.
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
        else:
            # Slower, kwargs-ready version.
            fields_iter = iter(opts.fields)
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
                if kwargs.pop(field.name, NOT_PROVIDED) is not NOT_PROVIDED:
                    raise TypeError(
                        f"{cls.__qualname__}() got both positional and "
                        f"keyword arguments for field '{field.name}'."
                    )
    
        # Now we're left with the unprocessed fields that *must* come from
        # keywords, or default.
    
        for field in fields_iter:
            is_related_object = False
            # Virtual field
            if field.column is None or field.generated:
                continue
            if kwargs:
                if isinstance(field.remote_field, ForeignObjectRel):
                    try:
                        # Assume object instance was passed in.
                        rel_obj = kwargs.pop(field.name)
                        is_related_object = True
                    except KeyError:
                        try:
                            # Object instance wasn't passed in -- must be an ID.
                            val = kwargs.pop(field.attname)
                        except KeyError:
                            val = field.get_default()
                else:
                    try:
                        val = kwargs.pop(field.attname)
                    except KeyError:
                        # This is done with an exception rather than the
                        # default argument on pop because we don't want
                        # get_default() to be evaluated, and then not used.
                        # Refs #12057.
                        val = field.get_default()
            else:
                val = field.get_default()
    
            if is_related_object:
                # If we are passed a related instance, set it using the
                # field.name instead of field.attname (e.g. "user" instead of
                # "user_id") so that the object gets properly cached (and type
                # checked) by the RelatedObjectDescriptor.
                if rel_obj is not _DEFERRED:
                    _setattr(self, field.name, rel_obj)
            else:
                if val is not _DEFERRED:
                    _setattr(self, field.attname, val)
    
        if kwargs:
            property_names = opts._property_names
            unexpected = ()
            for prop, value in kwargs.items():
                # Any remaining kwargs must correspond to properties or virtual
                # fields.
                if prop in property_names:
                    if value is not _DEFERRED:
                        _setattr(self, prop, value)
                else:
                    try:
                        opts.get_field(prop)
                    except FieldDoesNotExist:
                        unexpected += (prop,)
                    else:
                        if value is not _DEFERRED:
                            _setattr(self, prop, value)
            if unexpected:
                unexpected_names = ", ".join(repr(n) for n in unexpected)
>               raise TypeError(
                    f"{cls.__name__}() got unexpected keyword arguments: "
                    f"{unexpected_names}"
                )
E               TypeError: Turma() got unexpected keyword arguments: 'total_vagas'

..\.venv\Lib\site-packages\django\db\models\base.py:569: TypeError
_______ TestMatriculaService.test_avaliar_aluno_reprova_com_nota_baixa ________

self = <apps.academico.tests.test_services.TestMatriculaService testMethod=test_avaliar_aluno_reprova_com_nota_baixa>

    def setUp(self):
>       self.turma_com_vagas = TurmaFactory(total_vagas=5)

apps\academico\tests\test_services.py:10: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:661: in create
    obj = self.model(**kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <Turma: bar - Turma 9>, args = (), kwargs = {'total_vagas': 5}
cls = <class 'apps.eventos.models.Turma'>, opts = <Options for Turma>
_setattr = <built-in function setattr>, _DEFERRED = <Deferred field>
fields_iter = <tuple_iterator object at 0x0000025D5A9F9CC0>, val = None
field = <django.db.models.fields.DateTimeField: atualizado_em>
is_related_object = False, rel_obj = <Evento: bar>
property_names = frozenset({'pk'})

    def __init__(self, *args, **kwargs):
        # Alias some things as locals to avoid repeat global lookups
        cls = self.__class__
        opts = self._meta
        _setattr = setattr
        _DEFERRED = DEFERRED
        if opts.abstract:
            raise TypeError("Abstract models cannot be instantiated.")
    
        pre_init.send(sender=cls, args=args, kwargs=kwargs)
    
        # Set up the storage for instance state
        self._state = ModelState()
    
        # There is a rather weird disparity here; if kwargs, it's set, then args
        # overrides it. It should be one or the other; don't duplicate the work
        # The reason for the kwargs check is that standard iterator passes in by
        # args, and instantiation for iteration is 33% faster.
        if len(args) > len(opts.concrete_fields):
            # Daft, but matches old exception sans the err msg.
            raise IndexError("Number of args exceeds number of fields")
    
        if not kwargs:
            fields_iter = iter(opts.concrete_fields)
            # The ordering of the zip calls matter - zip throws StopIteration
            # when an iter throws it. So if the first iter throws it, the second
            # is *not* consumed. We rely on this, so don't change the order
            # without changing the logic.
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
        else:
            # Slower, kwargs-ready version.
            fields_iter = iter(opts.fields)
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
                if kwargs.pop(field.name, NOT_PROVIDED) is not NOT_PROVIDED:
                    raise TypeError(
                        f"{cls.__qualname__}() got both positional and "
                        f"keyword arguments for field '{field.name}'."
                    )
    
        # Now we're left with the unprocessed fields that *must* come from
        # keywords, or default.
    
        for field in fields_iter:
            is_related_object = False
            # Virtual field
            if field.column is None or field.generated:
                continue
            if kwargs:
                if isinstance(field.remote_field, ForeignObjectRel):
                    try:
                        # Assume object instance was passed in.
                        rel_obj = kwargs.pop(field.name)
                        is_related_object = True
                    except KeyError:
                        try:
                            # Object instance wasn't passed in -- must be an ID.
                            val = kwargs.pop(field.attname)
                        except KeyError:
                            val = field.get_default()
                else:
                    try:
                        val = kwargs.pop(field.attname)
                    except KeyError:
                        # This is done with an exception rather than the
                        # default argument on pop because we don't want
                        # get_default() to be evaluated, and then not used.
                        # Refs #12057.
                        val = field.get_default()
            else:
                val = field.get_default()
    
            if is_related_object:
                # If we are passed a related instance, set it using the
                # field.name instead of field.attname (e.g. "user" instead of
                # "user_id") so that the object gets properly cached (and type
                # checked) by the RelatedObjectDescriptor.
                if rel_obj is not _DEFERRED:
                    _setattr(self, field.name, rel_obj)
            else:
                if val is not _DEFERRED:
                    _setattr(self, field.attname, val)
    
        if kwargs:
            property_names = opts._property_names
            unexpected = ()
            for prop, value in kwargs.items():
                # Any remaining kwargs must correspond to properties or virtual
                # fields.
                if prop in property_names:
                    if value is not _DEFERRED:
                        _setattr(self, prop, value)
                else:
                    try:
                        opts.get_field(prop)
                    except FieldDoesNotExist:
                        unexpected += (prop,)
                    else:
                        if value is not _DEFERRED:
                            _setattr(self, prop, value)
            if unexpected:
                unexpected_names = ", ".join(repr(n) for n in unexpected)
>               raise TypeError(
                    f"{cls.__name__}() got unexpected keyword arguments: "
                    f"{unexpected_names}"
                )
E               TypeError: Turma() got unexpected keyword arguments: 'total_vagas'

..\.venv\Lib\site-packages\django\db\models\base.py:569: TypeError
_______________ TestMatriculaService.test_gerar_relatorio_turma _______________

self = <apps.academico.tests.test_services.TestMatriculaService testMethod=test_gerar_relatorio_turma>

    def setUp(self):
>       self.turma_com_vagas = TurmaFactory(total_vagas=5)

apps\academico\tests\test_services.py:10: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:661: in create
    obj = self.model(**kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <Turma: by - Turma 10>, args = (), kwargs = {'total_vagas': 5}
cls = <class 'apps.eventos.models.Turma'>, opts = <Options for Turma>
_setattr = <built-in function setattr>, _DEFERRED = <Deferred field>
fields_iter = <tuple_iterator object at 0x0000025D5A9FAFE0>, val = None
field = <django.db.models.fields.DateTimeField: atualizado_em>
is_related_object = False, rel_obj = <Evento: by>
property_names = frozenset({'pk'})

    def __init__(self, *args, **kwargs):
        # Alias some things as locals to avoid repeat global lookups
        cls = self.__class__
        opts = self._meta
        _setattr = setattr
        _DEFERRED = DEFERRED
        if opts.abstract:
            raise TypeError("Abstract models cannot be instantiated.")
    
        pre_init.send(sender=cls, args=args, kwargs=kwargs)
    
        # Set up the storage for instance state
        self._state = ModelState()
    
        # There is a rather weird disparity here; if kwargs, it's set, then args
        # overrides it. It should be one or the other; don't duplicate the work
        # The reason for the kwargs check is that standard iterator passes in by
        # args, and instantiation for iteration is 33% faster.
        if len(args) > len(opts.concrete_fields):
            # Daft, but matches old exception sans the err msg.
            raise IndexError("Number of args exceeds number of fields")
    
        if not kwargs:
            fields_iter = iter(opts.concrete_fields)
            # The ordering of the zip calls matter - zip throws StopIteration
            # when an iter throws it. So if the first iter throws it, the second
            # is *not* consumed. We rely on this, so don't change the order
            # without changing the logic.
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
        else:
            # Slower, kwargs-ready version.
            fields_iter = iter(opts.fields)
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
                if kwargs.pop(field.name, NOT_PROVIDED) is not NOT_PROVIDED:
                    raise TypeError(
                        f"{cls.__qualname__}() got both positional and "
                        f"keyword arguments for field '{field.name}'."
                    )
    
        # Now we're left with the unprocessed fields that *must* come from
        # keywords, or default.
    
        for field in fields_iter:
            is_related_object = False
            # Virtual field
            if field.column is None or field.generated:
                continue
            if kwargs:
                if isinstance(field.remote_field, ForeignObjectRel):
                    try:
                        # Assume object instance was passed in.
                        rel_obj = kwargs.pop(field.name)
                        is_related_object = True
                    except KeyError:
                        try:
                            # Object instance wasn't passed in -- must be an ID.
                            val = kwargs.pop(field.attname)
                        except KeyError:
                            val = field.get_default()
                else:
                    try:
                        val = kwargs.pop(field.attname)
                    except KeyError:
                        # This is done with an exception rather than the
                        # default argument on pop because we don't want
                        # get_default() to be evaluated, and then not used.
                        # Refs #12057.
                        val = field.get_default()
            else:
                val = field.get_default()
    
            if is_related_object:
                # If we are passed a related instance, set it using the
                # field.name instead of field.attname (e.g. "user" instead of
                # "user_id") so that the object gets properly cached (and type
                # checked) by the RelatedObjectDescriptor.
                if rel_obj is not _DEFERRED:
                    _setattr(self, field.name, rel_obj)
            else:
                if val is not _DEFERRED:
                    _setattr(self, field.attname, val)
    
        if kwargs:
            property_names = opts._property_names
            unexpected = ()
            for prop, value in kwargs.items():
                # Any remaining kwargs must correspond to properties or virtual
                # fields.
                if prop in property_names:
                    if value is not _DEFERRED:
                        _setattr(self, prop, value)
                else:
                    try:
                        opts.get_field(prop)
                    except FieldDoesNotExist:
                        unexpected += (prop,)
                    else:
                        if value is not _DEFERRED:
                            _setattr(self, prop, value)
            if unexpected:
                unexpected_names = ", ".join(repr(n) for n in unexpected)
>               raise TypeError(
                    f"{cls.__name__}() got unexpected keyword arguments: "
                    f"{unexpected_names}"
                )
E               TypeError: Turma() got unexpected keyword arguments: 'total_vagas'

..\.venv\Lib\site-packages\django\db\models\base.py:569: TypeError
______ TestMatriculaService.test_matricula_gerada_com_numero_sequencial _______

self = <apps.academico.tests.test_services.TestMatriculaService testMethod=test_matricula_gerada_com_numero_sequencial>

    def setUp(self):
>       self.turma_com_vagas = TurmaFactory(total_vagas=5)

apps\academico\tests\test_services.py:10: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:661: in create
    obj = self.model(**kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <Turma: goal - Turma 11>, args = (), kwargs = {'total_vagas': 5}
cls = <class 'apps.eventos.models.Turma'>, opts = <Options for Turma>
_setattr = <built-in function setattr>, _DEFERRED = <Deferred field>
fields_iter = <tuple_iterator object at 0x0000025D5A9FAB60>, val = None
field = <django.db.models.fields.DateTimeField: atualizado_em>
is_related_object = False, rel_obj = <Evento: goal>
property_names = frozenset({'pk'})

    def __init__(self, *args, **kwargs):
        # Alias some things as locals to avoid repeat global lookups
        cls = self.__class__
        opts = self._meta
        _setattr = setattr
        _DEFERRED = DEFERRED
        if opts.abstract:
            raise TypeError("Abstract models cannot be instantiated.")
    
        pre_init.send(sender=cls, args=args, kwargs=kwargs)
    
        # Set up the storage for instance state
        self._state = ModelState()
    
        # There is a rather weird disparity here; if kwargs, it's set, then args
        # overrides it. It should be one or the other; don't duplicate the work
        # The reason for the kwargs check is that standard iterator passes in by
        # args, and instantiation for iteration is 33% faster.
        if len(args) > len(opts.concrete_fields):
            # Daft, but matches old exception sans the err msg.
            raise IndexError("Number of args exceeds number of fields")
    
        if not kwargs:
            fields_iter = iter(opts.concrete_fields)
            # The ordering of the zip calls matter - zip throws StopIteration
            # when an iter throws it. So if the first iter throws it, the second
            # is *not* consumed. We rely on this, so don't change the order
            # without changing the logic.
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
        else:
            # Slower, kwargs-ready version.
            fields_iter = iter(opts.fields)
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
                if kwargs.pop(field.name, NOT_PROVIDED) is not NOT_PROVIDED:
                    raise TypeError(
                        f"{cls.__qualname__}() got both positional and "
                        f"keyword arguments for field '{field.name}'."
                    )
    
        # Now we're left with the unprocessed fields that *must* come from
        # keywords, or default.
    
        for field in fields_iter:
            is_related_object = False
            # Virtual field
            if field.column is None or field.generated:
                continue
            if kwargs:
                if isinstance(field.remote_field, ForeignObjectRel):
                    try:
                        # Assume object instance was passed in.
                        rel_obj = kwargs.pop(field.name)
                        is_related_object = True
                    except KeyError:
                        try:
                            # Object instance wasn't passed in -- must be an ID.
                            val = kwargs.pop(field.attname)
                        except KeyError:
                            val = field.get_default()
                else:
                    try:
                        val = kwargs.pop(field.attname)
                    except KeyError:
                        # This is done with an exception rather than the
                        # default argument on pop because we don't want
                        # get_default() to be evaluated, and then not used.
                        # Refs #12057.
                        val = field.get_default()
            else:
                val = field.get_default()
    
            if is_related_object:
                # If we are passed a related instance, set it using the
                # field.name instead of field.attname (e.g. "user" instead of
                # "user_id") so that the object gets properly cached (and type
                # checked) by the RelatedObjectDescriptor.
                if rel_obj is not _DEFERRED:
                    _setattr(self, field.name, rel_obj)
            else:
                if val is not _DEFERRED:
                    _setattr(self, field.attname, val)
    
        if kwargs:
            property_names = opts._property_names
            unexpected = ()
            for prop, value in kwargs.items():
                # Any remaining kwargs must correspond to properties or virtual
                # fields.
                if prop in property_names:
                    if value is not _DEFERRED:
                        _setattr(self, prop, value)
                else:
                    try:
                        opts.get_field(prop)
                    except FieldDoesNotExist:
                        unexpected += (prop,)
                    else:
                        if value is not _DEFERRED:
                            _setattr(self, prop, value)
            if unexpected:
                unexpected_names = ", ".join(repr(n) for n in unexpected)
>               raise TypeError(
                    f"{cls.__name__}() got unexpected keyword arguments: "
                    f"{unexpected_names}"
                )
E               TypeError: Turma() got unexpected keyword arguments: 'total_vagas'

..\.venv\Lib\site-packages\django\db\models\base.py:569: TypeError
______ TestMatriculaService.test_matricular_classificado_cria_matricula _______

self = <apps.academico.tests.test_services.TestMatriculaService testMethod=test_matricular_classificado_cria_matricula>

    def setUp(self):
>       self.turma_com_vagas = TurmaFactory(total_vagas=5)

apps\academico\tests\test_services.py:10: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:661: in create
    obj = self.model(**kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <Turma: close - Turma 12>, args = (), kwargs = {'total_vagas': 5}
cls = <class 'apps.eventos.models.Turma'>, opts = <Options for Turma>
_setattr = <built-in function setattr>, _DEFERRED = <Deferred field>
fields_iter = <tuple_iterator object at 0x0000025D5A9FBA00>, val = None
field = <django.db.models.fields.DateTimeField: atualizado_em>
is_related_object = False, rel_obj = <Evento: close>
property_names = frozenset({'pk'})

    def __init__(self, *args, **kwargs):
        # Alias some things as locals to avoid repeat global lookups
        cls = self.__class__
        opts = self._meta
        _setattr = setattr
        _DEFERRED = DEFERRED
        if opts.abstract:
            raise TypeError("Abstract models cannot be instantiated.")
    
        pre_init.send(sender=cls, args=args, kwargs=kwargs)
    
        # Set up the storage for instance state
        self._state = ModelState()
    
        # There is a rather weird disparity here; if kwargs, it's set, then args
        # overrides it. It should be one or the other; don't duplicate the work
        # The reason for the kwargs check is that standard iterator passes in by
        # args, and instantiation for iteration is 33% faster.
        if len(args) > len(opts.concrete_fields):
            # Daft, but matches old exception sans the err msg.
            raise IndexError("Number of args exceeds number of fields")
    
        if not kwargs:
            fields_iter = iter(opts.concrete_fields)
            # The ordering of the zip calls matter - zip throws StopIteration
            # when an iter throws it. So if the first iter throws it, the second
            # is *not* consumed. We rely on this, so don't change the order
            # without changing the logic.
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
        else:
            # Slower, kwargs-ready version.
            fields_iter = iter(opts.fields)
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
                if kwargs.pop(field.name, NOT_PROVIDED) is not NOT_PROVIDED:
                    raise TypeError(
                        f"{cls.__qualname__}() got both positional and "
                        f"keyword arguments for field '{field.name}'."
                    )
    
        # Now we're left with the unprocessed fields that *must* come from
        # keywords, or default.
    
        for field in fields_iter:
            is_related_object = False
            # Virtual field
            if field.column is None or field.generated:
                continue
            if kwargs:
                if isinstance(field.remote_field, ForeignObjectRel):
                    try:
                        # Assume object instance was passed in.
                        rel_obj = kwargs.pop(field.name)
                        is_related_object = True
                    except KeyError:
                        try:
                            # Object instance wasn't passed in -- must be an ID.
                            val = kwargs.pop(field.attname)
                        except KeyError:
                            val = field.get_default()
                else:
                    try:
                        val = kwargs.pop(field.attname)
                    except KeyError:
                        # This is done with an exception rather than the
                        # default argument on pop because we don't want
                        # get_default() to be evaluated, and then not used.
                        # Refs #12057.
                        val = field.get_default()
            else:
                val = field.get_default()
    
            if is_related_object:
                # If we are passed a related instance, set it using the
                # field.name instead of field.attname (e.g. "user" instead of
                # "user_id") so that the object gets properly cached (and type
                # checked) by the RelatedObjectDescriptor.
                if rel_obj is not _DEFERRED:
                    _setattr(self, field.name, rel_obj)
            else:
                if val is not _DEFERRED:
                    _setattr(self, field.attname, val)
    
        if kwargs:
            property_names = opts._property_names
            unexpected = ()
            for prop, value in kwargs.items():
                # Any remaining kwargs must correspond to properties or virtual
                # fields.
                if prop in property_names:
                    if value is not _DEFERRED:
                        _setattr(self, prop, value)
                else:
                    try:
                        opts.get_field(prop)
                    except FieldDoesNotExist:
                        unexpected += (prop,)
                    else:
                        if value is not _DEFERRED:
                            _setattr(self, prop, value)
            if unexpected:
                unexpected_names = ", ".join(repr(n) for n in unexpected)
>               raise TypeError(
                    f"{cls.__name__}() got unexpected keyword arguments: "
                    f"{unexpected_names}"
                )
E               TypeError: Turma() got unexpected keyword arguments: 'total_vagas'

..\.venv\Lib\site-packages\django\db\models\base.py:569: TypeError
_____ TestMatriculaService.test_verificar_disponibilidade_turma_com_vagas _____

self = <apps.academico.tests.test_services.TestMatriculaService testMethod=test_verificar_disponibilidade_turma_com_vagas>

    def setUp(self):
>       self.turma_com_vagas = TurmaFactory(total_vagas=5)

apps\academico\tests\test_services.py:10: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:661: in create
    obj = self.model(**kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <Turma: near - Turma 13>, args = (), kwargs = {'total_vagas': 5}
cls = <class 'apps.eventos.models.Turma'>, opts = <Options for Turma>
_setattr = <built-in function setattr>, _DEFERRED = <Deferred field>
fields_iter = <tuple_iterator object at 0x0000025D5A9434F0>, val = None
field = <django.db.models.fields.DateTimeField: atualizado_em>
is_related_object = False, rel_obj = <Evento: near>
property_names = frozenset({'pk'})

    def __init__(self, *args, **kwargs):
        # Alias some things as locals to avoid repeat global lookups
        cls = self.__class__
        opts = self._meta
        _setattr = setattr
        _DEFERRED = DEFERRED
        if opts.abstract:
            raise TypeError("Abstract models cannot be instantiated.")
    
        pre_init.send(sender=cls, args=args, kwargs=kwargs)
    
        # Set up the storage for instance state
        self._state = ModelState()
    
        # There is a rather weird disparity here; if kwargs, it's set, then args
        # overrides it. It should be one or the other; don't duplicate the work
        # The reason for the kwargs check is that standard iterator passes in by
        # args, and instantiation for iteration is 33% faster.
        if len(args) > len(opts.concrete_fields):
            # Daft, but matches old exception sans the err msg.
            raise IndexError("Number of args exceeds number of fields")
    
        if not kwargs:
            fields_iter = iter(opts.concrete_fields)
            # The ordering of the zip calls matter - zip throws StopIteration
            # when an iter throws it. So if the first iter throws it, the second
            # is *not* consumed. We rely on this, so don't change the order
            # without changing the logic.
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
        else:
            # Slower, kwargs-ready version.
            fields_iter = iter(opts.fields)
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
                if kwargs.pop(field.name, NOT_PROVIDED) is not NOT_PROVIDED:
                    raise TypeError(
                        f"{cls.__qualname__}() got both positional and "
                        f"keyword arguments for field '{field.name}'."
                    )
    
        # Now we're left with the unprocessed fields that *must* come from
        # keywords, or default.
    
        for field in fields_iter:
            is_related_object = False
            # Virtual field
            if field.column is None or field.generated:
                continue
            if kwargs:
                if isinstance(field.remote_field, ForeignObjectRel):
                    try:
                        # Assume object instance was passed in.
                        rel_obj = kwargs.pop(field.name)
                        is_related_object = True
                    except KeyError:
                        try:
                            # Object instance wasn't passed in -- must be an ID.
                            val = kwargs.pop(field.attname)
                        except KeyError:
                            val = field.get_default()
                else:
                    try:
                        val = kwargs.pop(field.attname)
                    except KeyError:
                        # This is done with an exception rather than the
                        # default argument on pop because we don't want
                        # get_default() to be evaluated, and then not used.
                        # Refs #12057.
                        val = field.get_default()
            else:
                val = field.get_default()
    
            if is_related_object:
                # If we are passed a related instance, set it using the
                # field.name instead of field.attname (e.g. "user" instead of
                # "user_id") so that the object gets properly cached (and type
                # checked) by the RelatedObjectDescriptor.
                if rel_obj is not _DEFERRED:
                    _setattr(self, field.name, rel_obj)
            else:
                if val is not _DEFERRED:
                    _setattr(self, field.attname, val)
    
        if kwargs:
            property_names = opts._property_names
            unexpected = ()
            for prop, value in kwargs.items():
                # Any remaining kwargs must correspond to properties or virtual
                # fields.
                if prop in property_names:
                    if value is not _DEFERRED:
                        _setattr(self, prop, value)
                else:
                    try:
                        opts.get_field(prop)
                    except FieldDoesNotExist:
                        unexpected += (prop,)
                    else:
                        if value is not _DEFERRED:
                            _setattr(self, prop, value)
            if unexpected:
                unexpected_names = ", ".join(repr(n) for n in unexpected)
>               raise TypeError(
                    f"{cls.__name__}() got unexpected keyword arguments: "
                    f"{unexpected_names}"
                )
E               TypeError: Turma() got unexpected keyword arguments: 'total_vagas'

..\.venv\Lib\site-packages\django\db\models\base.py:569: TypeError
_____ TestMatriculaService.test_verificar_disponibilidade_turma_sem_vagas _____

self = <apps.academico.tests.test_services.TestMatriculaService testMethod=test_verificar_disponibilidade_turma_sem_vagas>

    def setUp(self):
>       self.turma_com_vagas = TurmaFactory(total_vagas=5)

apps\academico\tests\test_services.py:10: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
..\.venv\Lib\site-packages\factory\base.py:43: in __call__
    return cls.create(**kwargs)
..\.venv\Lib\site-packages\factory\base.py:539: in create
    return cls._generate(enums.CREATE_STRATEGY, kwargs)
..\.venv\Lib\site-packages\factory\django.py:122: in _generate
    return super()._generate(strategy, params)
..\.venv\Lib\site-packages\factory\base.py:468: in _generate
    return step.build()
..\.venv\Lib\site-packages\factory\builder.py:274: in build
    instance = self.factory_meta.instantiate(
..\.venv\Lib\site-packages\factory\base.py:320: in instantiate
    return self.factory._create(model, *args, **kwargs)
..\.venv\Lib\site-packages\factory\django.py:175: in _create
    return manager.create(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\manager.py:87: in manager_method
    return getattr(self.get_queryset(), name)(*args, **kwargs)
..\.venv\Lib\site-packages\django\db\models\query.py:661: in create
    obj = self.model(**kwargs)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

self = <Turma: wait - Turma 14>, args = (), kwargs = {'total_vagas': 5}
cls = <class 'apps.eventos.models.Turma'>, opts = <Options for Turma>
_setattr = <built-in function setattr>, _DEFERRED = <Deferred field>
fields_iter = <tuple_iterator object at 0x0000025D5A899990>, val = None
field = <django.db.models.fields.DateTimeField: atualizado_em>
is_related_object = False, rel_obj = <Evento: wait>
property_names = frozenset({'pk'})

    def __init__(self, *args, **kwargs):
        # Alias some things as locals to avoid repeat global lookups
        cls = self.__class__
        opts = self._meta
        _setattr = setattr
        _DEFERRED = DEFERRED
        if opts.abstract:
            raise TypeError("Abstract models cannot be instantiated.")
    
        pre_init.send(sender=cls, args=args, kwargs=kwargs)
    
        # Set up the storage for instance state
        self._state = ModelState()
    
        # There is a rather weird disparity here; if kwargs, it's set, then args
        # overrides it. It should be one or the other; don't duplicate the work
        # The reason for the kwargs check is that standard iterator passes in by
        # args, and instantiation for iteration is 33% faster.
        if len(args) > len(opts.concrete_fields):
            # Daft, but matches old exception sans the err msg.
            raise IndexError("Number of args exceeds number of fields")
    
        if not kwargs:
            fields_iter = iter(opts.concrete_fields)
            # The ordering of the zip calls matter - zip throws StopIteration
            # when an iter throws it. So if the first iter throws it, the second
            # is *not* consumed. We rely on this, so don't change the order
            # without changing the logic.
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
        else:
            # Slower, kwargs-ready version.
            fields_iter = iter(opts.fields)
            for val, field in zip(args, fields_iter):
                if val is _DEFERRED:
                    continue
                _setattr(self, field.attname, val)
                if kwargs.pop(field.name, NOT_PROVIDED) is not NOT_PROVIDED:
                    raise TypeError(
                        f"{cls.__qualname__}() got both positional and "
                        f"keyword arguments for field '{field.name}'."
                    )
    
        # Now we're left with the unprocessed fields that *must* come from
        # keywords, or default.
    
        for field in fields_iter:
            is_related_object = False
            # Virtual field
            if field.column is None or field.generated:
                continue
            if kwargs:
                if isinstance(field.remote_field, ForeignObjectRel):
                    try:
                        # Assume object instance was passed in.
                        rel_obj = kwargs.pop(field.name)
                        is_related_object = True
                    except KeyError:
                        try:
                            # Object instance wasn't passed in -- must be an ID.
                            val = kwargs.pop(field.attname)
                        except KeyError:
                            val = field.get_default()
                else:
                    try:
                        val = kwargs.pop(field.attname)
                    except KeyError:
                        # This is done with an exception rather than the
                        # default argument on pop because we don't want
                        # get_default() to be evaluated, and then not used.
                        # Refs #12057.
                        val = field.get_default()
            else:
                val = field.get_default()
    
            if is_related_object:
                # If we are passed a related instance, set it using the
                # field.name instead of field.attname (e.g. "user" instead of
                # "user_id") so that the object gets properly cached (and type
                # checked) by the RelatedObjectDescriptor.
                if rel_obj is not _DEFERRED:
                    _setattr(self, field.name, rel_obj)
            else:
                if val is not _DEFERRED:
                    _setattr(self, field.attname, val)
    
        if kwargs:
            property_names = opts._property_names
            unexpected = ()
            for prop, value in kwargs.items():
                # Any remaining kwargs must correspond to properties or virtual
                # fields.
                if prop in property_names:
                    if value is not _DEFERRED:
                        _setattr(self, prop, value)
                else:
                    try:
                        opts.get_field(prop)
                    except FieldDoesNotExist:
                        unexpected += (prop,)
                    else:
                        if value is not _DEFERRED:
                            _setattr(self, prop, value)
            if unexpected:
                unexpected_names = ", ".join(repr(n) for n in unexpected)
>               raise TypeError(
                    f"{cls.__name__}() got unexpected keyword arguments: "
                    f"{unexpected_names}"
                )
E               TypeError: Turma() got unexpected keyword arguments: 'total_vagas'

..\.venv\Lib\site-packages\django\db\models\base.py:569: TypeError
-------------------------- Captured stderr teardown ---------------------------
Destroying test database for alias 'default'...

=========================== short test summary info ===========================
FAILED apps/academico/tests/test_models.py::TestAvaliacao::test_avaliacao_aprovado_quando_nota_maior_7
FAILED apps/academico/tests/test_models.py::TestAvaliacao::test_avaliacao_criada_corretamente
FAILED apps/academico/tests/test_models.py::TestAvaliacao::test_avaliacao_reprovado_quando_nota_menor_7
FAILED apps/academico/tests/test_services.py::TestMatriculaService::test_alterar_status_matricula
FAILED apps/academico/tests/test_services.py::TestMatriculaService::test_avaliar_aluno_aprova_com_nota_alta
FAILED apps/academico/tests/test_services.py::TestMatriculaService::test_avaliar_aluno_reprova_com_nota_baixa
FAILED apps/academico/tests/test_services.py::TestMatriculaService::test_gerar_relatorio_turma
FAILED apps/academico/tests/test_services.py::TestMatriculaService::test_matricula_gerada_com_numero_sequencial
FAILED apps/academico/tests/test_services.py::TestMatriculaService::test_matricular_classificado_cria_matricula
FAILED apps/academico/tests/test_services.py::TestMatriculaService::test_verificar_disponibilidade_turma_com_vagas
FAILED apps/academico/tests/test_services.py::TestMatriculaService::test_verificar_disponibilidade_turma_sem_vagas
======================== 11 failed, 6 passed in 5.74s =========================
